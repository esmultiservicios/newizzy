<?php

    // echo "No se pudo obtener la versión.";
    echo 'Versión: 5.0.0';

    /*
     * Número de Versión Principal (Major Version): El primer número representa la versión principal del software. Se incrementa cuando se realizan cambios significativos que pueden no ser compatibles con versiones anteriores. Por ejemplo, agregar una funcionalidad importante o realizar cambios que rompen la compatibilidad hacia atrás.
     *
     * Número de Versión Menor (Minor Version): El segundo número indica la versión menor. Se incrementa cuando se agregan nuevas funcionalidades o mejoras que son compatibles con versiones anteriores. Por ejemplo, agregar una nueva función sin romper nada existente.
     *
     * Número de Versión de Parche (Patch Version): El tercer número representa la versión de parche. Se incrementa cuando se realizan correcciones de errores o parches que no agregan nuevas funcionalidades ni cambian la compatibilidad con versiones anteriores.
     *
     * Entonces, en "2.0.10", la interpretación sería la siguiente:
     *
     * 2 es la versión principal, lo que significa que se han realizado cambios importantes en el software desde la versión 1.
     * 0 es la versión menor, lo que indica que se han realizado mejoras y adiciones de funcionalidades desde la versión 2.0.0.
     * 10 es la versión de parche, lo que significa que se han realizado 10 correcciones de errores desde la versión 2.0.0.
     * Este enfoque permite a los usuarios del software entender rápidamente qué tipo de cambios se han realizado en una versión y si es probable que sea compatible con su uso existente. Es una práctica común en la industria del software para mantener un control claro de las versiones y comunicar los cambios a los usuarios.
     */
